document:
  name: api-gateway

spring:
  cloud:
    gateway:
      routes:
        - id: document-service-route
          uri: lb://document-service
          predicates:
          - Path=/api/documents/**

management:
  endpoints:
    web:
      exposure:
        include: "*"